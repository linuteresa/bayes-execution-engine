# --- Orchestration ---
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.1.0   # OpenAI-compatible client used to talk to llama.cpp's server

# --- Bayesian engine ---
pgmpy>=0.1.25             # optional: exact inference / network visualisation
numpy>=1.24
scipy>=1.10               # exact Beta credible intervals (degrades gracefully if absent)

# --- Scaling proof-of-concept ---
scikit-learn>=1.3         # PCA / manifold latent projection

# --- Core / config ---
pydantic>=2.0
python-dotenv>=1.0

# --- Web + async API ---
flask>=2.3.0              # simple synchronous demo UI
fastapi>=0.110            # async submit/poll service
uvicorn[standard]>=0.27   # ASGI server for FastAPI

# --- Tests / lint ---
pytest>=7.0

# --- Optional, enabled by env (uncomment to install) ---
# redis>=5.0                       # durable job store + checkpoints
# langgraph-checkpoint-redis       # Redis LangGraph saver
# langgraph-checkpoint-postgres    # Postgres LangGraph saver
# opentelemetry-sdk                # distributed tracing export
